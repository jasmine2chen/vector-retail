# vector-retail v2.0 — Runtime Dependencies
# Install: pip install -r requirements.txt

# HTTP server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# LLM / Agent framework
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.3.0

# Data validation
pydantic>=2.7.0

# Market data
yfinance>=0.2.40
requests>=2.32.0

# Authentication (JWT)
python-jose[cryptography]>=3.3.0

# Structured logging (OpenTelemetry-compatible output)
structlog>=24.0.0

# Resilience / retry / circuit breakers
tenacity>=9.0.0

# Numerical / risk computation
numpy>=1.26.0

# Environment configuration
python-dotenv>=1.0.0

# ── Production extras (uncomment as needed) ────────────────────────────
# opentelemetry-sdk>=1.24.0                  # Full OTel traces
# opentelemetry-exporter-otlp>=1.24.0       # Export to Jaeger/Grafana
# psycopg2-binary>=2.9.9                     # PostgreSQL session persistence
# redis>=5.0.0                               # Distributed quote cache
# prometheus-client>=0.20.0                 # Metrics /metrics endpoint
# boto3>=1.34.0                             # S3 WORM audit log export
# presidio-analyzer>=2.2.0                  # Production-grade PII NER
# presidio-anonymizer>=2.2.0
